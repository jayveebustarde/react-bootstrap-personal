import React from "react";
import { Container } from "react-bootstrap";


const Resume =() => {
    return (
        <Container>
            test resume
        </Container>
    );
};

export default Resume;